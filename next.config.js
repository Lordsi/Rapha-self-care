/** @type {import('next').NextConfig} */
const nextConfig = {
  // Serve assets from the legacy /assets folder at the root
  // (images, PDFs, favicons etc. already live there)
};

module.exports = nextConfig;
